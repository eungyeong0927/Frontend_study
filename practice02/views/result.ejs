<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
        <style>
            span {
                color: blue;
                font-weight: 700;
            }
        </style>
    </head>
    <body>
        <h1><%= title %></h1>
        <div><span><%= userInfo.name%></span>님 환영합니다!</div>
        <div>성별은 <span><%= userInfo.gender%></span>이군요!!</div>
        <div>
            생년월일은
            <span><%= userInfo.birth_year%></span>년 <span><%= userInfo.birth_month%></span>월 <span><%= userInfo.birth_date%></span>일 입니다.
        </div>

        <div>관심사는 <span><%= userInfo.interest%></span>입니다.</div>
        <div>
            좋아하는 색은 <span style="display: inline-block; width: 20px; height: 20px; /* background-color: <%= userInfo.color%> */"></span>입니다.
        </div>
        <div>좋아하는 숫자는 <span><%= userInfo.number%></span>입니다.</div>

        <br />
        <br />
        <a href="/">홈으로 이동하기</a>
    </body>
</html>
