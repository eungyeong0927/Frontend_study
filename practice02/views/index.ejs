<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
    </head>
    <body>
        <h1><%= title%></h1>
        <form action="/postForm" method="post">
            <label for="name">이름</label>
            <input type="text" name="name" id="name" required />
            <fieldset>
                <legend>성별</legend>
                <input type="radio" name="gender" value="여자" id="female" required />
                <label for="female">여자</label>
                <input type="radio" name="gender" value="남자" id="male" />
                <label for="male">남자</label>
            </fieldset>
            <fieldset>
                <legend>생년월일</legend>
                <select name="birth_year" id="birth_year">
                    <% for(let i=1900; i<2024; i++) { %>
                    <option value="<%= i%>"><%= i%></option>
                    <% }%></select
                >년
                <select name="birth_month" id="birth_month">
                    <% for(let i=1; i<13; i++) { %>
                    <option value="<%= i%>"><%= i%></option>
                    <% } %></select
                >월
                <select name="birth_date" id="birth_date">
                    <% for(let i=1; i<32; i++) { %>
                    <option value="<%= i%>"><%= i%></option>
                    <% } %></select
                >일
            </fieldset>

            <fieldset>
                <legend>관심사</legend>
                <label><input type="checkbox" name="interest" value="basketball" checked />여행</label>
                <label><input type="checkbox" name="interest" value="movie" />패션</label>
                <label><input type="checkbox" name="interest" value="lay-down" />음식</label>
            </fieldset>
            <fieldset>
                <legend>기타 선택</legend>
                <div>
                    좋아하는 색상
                    <input type="color" id="color" name="color" value="#e66465" />
                    좋아하는 숫자
                    <input type="number" id="number" name="number" min="0" max="10" />
                </div>
            </fieldset>
            <button type="submit">회원가입</button>
        </form>
    </body>
</html>
